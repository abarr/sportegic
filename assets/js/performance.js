let Performance = {
    form_setup(M){
        let selects = document.querySelectorAll('select');
        M.FormSelect.init(selects, {});
    }
}
export default Performance                                                                                                  