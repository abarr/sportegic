<div class="row page">
  <div class="col s12">
    <div class="section heading">
      <div class="row remove-bottom-margin">
        <div class="col s12"> 
          <h5 class="heading grey-text lighten-2">Notes</h5>
        </div>
      </div>
      <div class="divider"></div>
    </div>
    <div class="row page remove-bottom-margin">
      <div id="vue-search-results">
        
        <div class="col s12">
          <div class="row">
            <div class="input-field col s12">
              <i class="material-icons prefix">search</i>
              <input id="icon_prefix" type="text" class="validate" v-model="search" v-on:keyup="searchNotes">
              <label for="icon_prefix">Search all notes</label>
            </div>
          </div>
        </div>

        <div class="row page" >
          <div class="col s12">
            <table class="highlight responsive-table">
              
              <thead v-if="notes.length > 0">
                <tr>
                    <th>Event</th>
                    <th>Created</th>
                    <th>Subject</th>
                    <th>Owner</th>
                    <th>Tags</th>
                    <th>People</th>
                    
                </tr>
              </thead>
              <tbody v-for="note in notes">
            
                <tr>
                  <a href="/notes/1">
                  <td > {{ note.event_date | moment }}</td>
                  <td> {{ note.inserted_at | moment}} </td>
                  <td> {{ note.subject }} </td>
                  <td> {{ note.user.firstname }} {{ note.user.lastname }} </td>
                  <td >
                      <div class="chip truncate" v-for="tag in note.types">
                        {{tag.name}}
                      </div>
                  </td>
                  <td >
                      <div class="chip truncate" v-for="person in note.people">
                        {{ person.firstname }} {{ person.lastname }}
                      </div>
                  </td>
                  
                </a>  
                </tr>
                
              </tbody>
            </table>
</div>
      </div>


      </div>
    
    
      
    </div>
          
</div>

<div>
    <div class="fixed-action-btn ">
      <%= link to: Routes.note_path(@conn, :new), class: "btn-floating btn-large blue"  do %>
        <i class="large material-icons">add</i>
      <% end %>
    </div>
</div>

<script>
 
</script>










